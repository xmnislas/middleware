package middleware;

/**
 *
 * @author Josue
 */
public class Main {
    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Middleware servidor = new Middleware();
    }
}
